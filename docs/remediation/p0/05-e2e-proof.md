# Preuves E2E P0

Playwright 1.61.1 et Chromium sont installés avec desktop/mobile, traces, captures et vidéos sur échec.

Dernière exécution Chromium : **8 réussis, 2 échoués**.

Réussis : unknown/unavailable, vrai zéro, devises, division par zéro, erreur/vide, fallback mock neutralisé, verrou API d’export, verrou impression, loader sur vue publique.

Échecs bloquants : test anonyme expiré faute d’accès réseau à Supabase ; comptes `TEST_ORG_A_*` et `TEST_ORG_B_*` absents pour l’isolation.
